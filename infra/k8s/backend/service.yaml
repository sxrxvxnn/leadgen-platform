apiVersion: v1
kind: Service
metadata:
  name: leadgen-backend
  namespace: leadgen
  labels:
    app: leadgen-backend
spec:
  selector:
    app: leadgen-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
